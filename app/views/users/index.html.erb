<div class="all_user_display">

  <h1>TravelFlowers</h1>
    <ul>
      <% @users.each do |user| %>
        <li>
          <%= link_to image_tag(user.gravatar_url), user_path(user) %> 
          <%= user.name %><br>
          <% end %>
        </li>
      <br>
    </ul>  
      
</div>
